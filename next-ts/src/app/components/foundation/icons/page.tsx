import { CONFIG } from 'src/config-global';

import { IconsView } from 'src/sections/_examples/foundation/icons-view';

// ----------------------------------------------------------------------

export const metadata = { title: `Icons | Foundations - ${CONFIG.site.name}` };

export default function Page() {
  return <IconsView />;
}
